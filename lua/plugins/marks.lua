return {
  "kshenoy/vim-signature",
  enabled = true,
}

--[[
Create Mark (m + character)
Delete Mark (dm + character)
Jump to Mark Line (' + character)
Jump to Mark Col (` + character`)

Jump to next mark in file (]')
Jump to prev mark in file ([')
Replace ' with ` to cycle between col marks


--
--]]
